/**
 *
 * by littlefean
 */

// let LINE_COLOR = "rgba(0, 0, 0, 0.2)";
let LINE_COLOR = "rgba(255, 255, 255, 0.1)";
let RED_COLOR = "rgba(255, 0, 0, 0.8)"
// let CIRCLE_BODY_COLOR = "rgba(109, 109, 109, 1)";
let CIRCLE_BODY_COLOR = "rgba(200, 200, 200, 1)";

// 躁动系数
let HOT = 1;
// 最小连接数量
let MIN_CONNECT = 8;
// 节点的数量
let NODE_NUMBER = 50;
// 最短路径开始节点号
let MIN_LEN_START_NODE = 0;

// 显示节点
let SHOW_BALL = true;
//
let SHOW_LINE = true;

let SHOW_TREE = false;

let DFS_SPEED = 1;
let DFS_DURATION = 10;
